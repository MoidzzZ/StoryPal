name: storypal-chatbot
channels:
  - defaults
dependencies:
  - python=3.12
  - pip>=24
  - pip:
      - pytest>=9.0.0,<10.0.0
      - pytest-asyncio>=1.3.0,<2.0.0

# nanobot itself is intentionally not fetched from PyPI here. Build and install
# the fixed source under .reference/nanobot first, then install ./chatbot in
# editable mode. This preserves the audited upstream commit.
