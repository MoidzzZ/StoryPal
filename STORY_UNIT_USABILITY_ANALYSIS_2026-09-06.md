# 《流浪地球》故事单元内容可用性分析

- 分析日期：2026-09-06
- 数据范围：`story_mem/data/wandering_earth/03_extracted/units_extracted.jsonl`，完整 LLM 抽取的 108 个单元
- 目的：判断这些单元能否支撑首次阅读陪读；明确哪些字段能进入产品，哪些只能留在内部；把后续实现拆成可验证任务。

## 结论先行

这批数据已经足以支撑“已读范围内的解释、定位与带证据回应”，但还不适合把抽取字段直接做成百科页、人物卡或剧情摘要页。最合适的首个产品闭环不是让用户浏览知识库，而是让聊天机器人在用户读到某处时，能安全地：解释一个名词、帮用户回到刚读过的情节、回应感受并保存经确认的问题或猜测。

推荐状态如下：

| 范围 | 结论 | 原因 |
| --- | --- | --- |
| 已读范围检索 + 回答证据 | 可以推进 | 108 个单元均有原文、摘要、近因事件与状态快照；检索层已有 FTS5、LanceDB 与阅读边界过滤。 |
| 章节/段落定位 + 短回顾 | 可在阅读位置映射完成后推进 | `chapter_idx`、单元顺序、起止行号齐全；但用户的“读到哪里”还没有友好输入与半个单元的处理。 |
| 阅读手账（反应、问题、预测） | 可独立实现 | 它存的是用户确认内容，不依赖自动从故事数据抽取人格或观点。 |
| 人物卡、地点卡、术语卡 | 暂不直接上线 | 原始标签存在概念、组织、关系组合与别名混杂，未清洗前会误导用户。 |
| 全局剧情总结、后续提示、自动预测核验 | 禁止 | 会绕过首次阅读边界，或把模型推断当作作品事实。 |

## 数据事实与覆盖率

- 共 108 个单元，覆盖“刹车时代”26 个、“逃逸时代”51 个、“叛乱”26 个、“流浪时代”5 个。
- 每个单元都不是降级结果（`degraded=false`），都有 `summary`、`text`、`recent_event` 与 `state_snapshot`。
- 原文片段平均 214.2 个字符，中位数 190，最短 53、最长 673；适合定位与内部取证，不应默认整段直接推给用户。
- 77/108 单元有 `characters`，58/108 有 `locations`，84/108 有 `key_terms`；103/108 有 `entity_updates`，82/108 有 `plotline_updates`，86/108 有 `context_refs`。
- 高频术语是“太阳”(16)、“地球”(11)、“地球发动机”(6)；高频地点是“地下城”(7)、“地球”(5)、“市中心广场”(4)、“木星”(4)。这说明术语和地点可作为检索扩展线索，但还不是面向用户的目录。

## 字段分层与展示规则

| 字段 | 当前用途 | 是否直接展示 |
| --- | --- | --- |
| `work_id`、`unit_id`、`order`、章节与行号 | 作品隔离、阅读边界、来源定位 | 仅将章节/位置翻译为用户友好的“你读到……附近”；`unit_id`、顺序与行号不展示。 |
| `text` | 最终事实依据、按需小段引用 | 默认不整段展示；只有用户要求原文或需要澄清时，截取已读范围中的必要部分。 |
| `summary`、`recent_event` | 检索后的内部理解与简短回答依据 | 不原样展示。它们可帮模型组织语言，但回答必须回到可定位的已读证据。 |
| `characters`、`locations`、`key_terms` | 候选召回、查询扩展 | 不做原始标签页；清洗、别名归一和类别校验后才考虑做卡片。 |
| `entity_updates`、`plotline_updates`、`state_snapshot`、`context_refs` | 后续一致性校验、前文关联、测试素材 | 内部字段。先验证准确性、时间边界和跨单元一致性，再逐项开放。 |
| `degraded` | 数据健康检查 | 内部监控。 |

## 已发现的质量与产品风险

1. `characters` 不是严格“人物”字段。共有 58 个不同标签，其中已经出现“主角与加代子”“主角与山彬加代子”“受伤和没受伤的人”等关系或群体标签；“叛军”“联合政府”也属于组织而非人物。因此不能用该字段生成“人物出现次数”或人物关系图。
2. 地点、概念和舞台可能混用。例如“地球”“天空”等既可能是广义场景，也可能是叙述对象；需要层级和类别，不能把所有标签都当作可导航地点。
3. 单元边界不是读者段落边界。若用户读到某单元中部，只用 `order <= max_seen_order` 会把同一单元尚未读到的末尾内容一并允许。因此阅读定位至少要支持“已完成单元”，或增加精确的行/段落锚点后再开放细粒度定位。
4. 抽取摘要密度高。即使单元本身在已读范围，直接呈现摘要也可能比读者自己刚读的节奏更快，造成不必要的剧透感或替代阅读。
5. `unit_id` 依赖当前切分版本。将来重新分块后可能变化；任何要长期保存的阅读手账不能只引用 `unit_id`，还要保存作品版本/源哈希、锚点文本或可迁移位置。
6. 向量检索必须在候选生成阶段就按阅读边界过滤；不能先让模型看未来候选再要求它“不说”。FTS、向量、相邻扩展、RRF 与按 ID 读取都要遵守同一条规则。

## 最小可用陪读场景

### 1. 读到一个概念时的即时解释

用户：“地球发动机为什么能让地球走？”

系统只检索当前作品、当前已读位置以前的单元；以原文片段为证据，用自然语言解释；若范围内没有足够依据，明确说“你读到的部分还没有解释清楚”，而不是补全后续设定。

### 2. 用户说“刚才那段我没看懂”

先根据最近对话、阅读锚点或用户提供的词语消歧；取不超过少量的已读单元；回答时提供“要不要我带你回到……这一段”的友好定位，而不是显示内部单元编号。

### 3. 陪伴阅读与手账

对“这里好压抑”“我怀疑某人会……”先回应感受或讨论依据；仅在用户明确同意后，写入独立阅读手账。手账不是模型对用户的画像，也不是自动剧情总结。

建议的最小字段：`entry_id`、`active_work`、`source_version`、`anchor_order`、`anchor_text`、`entry_type`（反应/问题/预测）、`content`、`created_at`、`status`、`resolved_at`。每次读取、展示、核验都以 `active_work` 与阅读边界为数据层过滤条件。

## 接入前验收清单

1. 先完成阅读位置模型：明确“章节开始/单元完成/精确锚点”三种状态，未完成前只允许按完整单元设边界。
2. 给 `search_story` 增加策略可观察性：返回本次使用 FTS 或向量、候选数、边界过滤数和最终来源；这些信息只用于日志与测试。
3. 建一组中文防剧透场景：术语解释、指代追问、同义改写、情绪回应、不足证据、用户试探后续、单元中部锚点。每例检查所有证据都不超过边界。
4. 新建阅读手账存储，与 `Notes` 完全分开；`Notes` 只保留用户主动确认的稳定偏好。
5. 先做术语/人物标签清洗审计：类型（人物/组织/群体/概念）、规范名、别名、来源单元、置信度。清洗完成前不做可浏览知识卡。
6. 等至少十条真实 Luna 陪读轨迹后，再决定是否做自动回顾、预测核验界面或 HistoryMemory 扩展。

## 两周内的推荐顺序

- 第 1 周前半：在 Chatbot 适配层接入 `auto`（向量优先、FTS 降级），保持边界过滤；补策略与防剧透回归。
- 第 1 周后半：实现阅读位置输入和短回顾，只覆盖完整已读单元；用真实用户语言验收。
- 第 2 周前半：实现独立阅读手账 v1，支持用户确认写入、查看、删除与已读范围内的预测核验。
- 第 2 周后半：收集真实陪读轨迹，完成标签清洗审计；根据结果再决定知识卡、RRF 或 HistoryMemory 的优先级。

本分析不改变协作仓库的抽取事实、索引或数据格式；它只定义 Chatbot 如何安全消费这些数据。